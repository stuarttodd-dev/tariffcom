describe.skip('UserList.vue', () => {
    it('shows Create User button on the page', () => {
    })

    it('has goToCreateUser function and button click handler', () => {
    })

    it('displays user data in table', () => {
    })

    it('shows search input', () => {
    })

    it('shows trashed users button', () => {
    })

    it('displays user type badges correctly', () => {
    })

    it('shows action buttons for each user', () => {
    })
})
